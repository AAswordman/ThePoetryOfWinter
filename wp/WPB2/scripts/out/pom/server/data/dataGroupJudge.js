export function dataGroupJudge(client, judge) {
    if (!judge.groupCondition)
        return false;
    if (judge.groupBasedOn === "tag") {
        for (let i of judge.groupCondition) {
        }
    }
}
//# sourceMappingURL=dataGroupJudge.js.map